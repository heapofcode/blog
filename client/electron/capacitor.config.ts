import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'blog.ionic.starter',
  appName: 'blog',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
